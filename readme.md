# Backend of the LocalShowBrowser
Contains the database and the backend itself.

## Installation
1. Install Python 3.10.6 (Other version may work also)
2. Run: <pre>pip install -r requirements.txt</pre>
3. Start app.py